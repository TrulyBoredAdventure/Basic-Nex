{
  "name": "basic-nex",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Alt1 solo and duo Nex encounter helper.",
  "author": "TrulyBoredAdventure",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/engine.js && node --check src/chat-reader.js && node --check src/encounter-data.js"
  }
}
